{
  "name": "@involve/api-dynamics",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Azure Function proxy for Dynamics 365 — holds the client secret, serves all apps in the suite.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@involve/config": "*",
    "@involve/shared": "*",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
