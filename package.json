{
  "name": "involve-dynamics-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Suite of PWAs and shared Azure Function proxy for Dynamics 365 (Involve).",
  "workspaces": [
    "apps/*",
    "packages/*",
    "api/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": "^20 || ^22"
  }
}
